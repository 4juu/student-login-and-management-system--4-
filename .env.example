# ⚠️ لا تضع مفاتيح حقيقية هنا أبداً
# انسخ هذا الملف إلى .env واملأ المفاتيح محلياً فقط
# (ملف .env مستثنى من git ولا يُرفع إلى المستودع)

# 🔗 عنوان خادم استخراج اسم البطاقة (Flask + OpenCV + Tesseract)
# شغّل الخادم من مجلد backend:  pip install -r requirements.txt ثم python app.py
VITE_NAME_API_URL=http://localhost:5000

# 📡 Sentry (مراقبة الأخطاء في الإنتاج) — اتركه فارغاً لتعطيله
# أنشئ مشروعاً على sentry.io ثم الصق الـ DSN هنا
VITE_SENTRY_DSN=
